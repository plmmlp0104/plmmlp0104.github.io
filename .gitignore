# local tooling / config
.claude/

# source captures & duplicates (not used by the site)
assets/canva/
증사.jpg

# os / editor
.DS_Store
Thumbs.db
node_modules/

# large original work images (compressed copies live in assets/work)
카드/
